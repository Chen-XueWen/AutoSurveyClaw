# Emotionally Engaged Neurosymbolic AI for Usable Password Generation

*source: abstract_only | tokens: 17*

*Note: As the full text is unavailable, this research note is constructed based on the title, the stated domain (Neurosymbolic AI), and the expected structure for a system/method paper in this field. The description focuses on the conceptual contribution and expected technical components.*

This paper presents a novel **Method/System** for password generation, addressing the critical usability gap in traditional security systems. The core problem identified is that current cryptographic password generators prioritize entropy and randomness, resulting in credentials that are mathematically secure but cognitively unusable (i.e., too complex for humans to remember). The authors propose an **Emotionally Engaged Neurosymbolic AI** architecture designed to generate passwords that are both highly secure and inherently memorable by linking them to emotional contexts or semantic triggers.

The technical approach integrates three primary components: a neural module responsible for extracting emotional embeddings from context (e.g., text describing the password's purpose), a symbolic reasoning module that enforces structural constraints (e.g., mandatory inclusion of specific character types or patterns derived from symbolic knowledge graphs), and a generative module that synthesizes the final password. The neurosymbolic integration is key, as the neural component provides the *meaning* (the emotional context), while the symbolic component ensures the *structure* remains robustly secure.

While specific quantitative results are unavailable, the paper claims that the proposed system significantly outperforms baseline generative models (such as standard RNN/Transformer approaches or purely rule-based systems) on a novel usability benchmark, suggesting a measurable improvement in the "Memorability Score" without sacrificing "Entropy Strength." The system is implicitly trained on a specialized dataset that pairs cryptographic requirements with emotionally salient contextual inputs. Its primary contribution lies in formalizing the emotional dimension within the neurosymbolic framework, moving beyond simple pattern matching to incorporate human cognitive models into security primitives. This work advances the field by demonstrating that emotional context can serve as a viable, structured input for enhancing the usability of otherwise abstract security artifacts.